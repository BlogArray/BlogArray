import { TwFormDirective } from './form.directive';

describe('TwFormDirective', () => {
  it('should create an instance', () => {
    const directive = new TwFormDirective();
    expect(directive).toBeTruthy();
  });
});
